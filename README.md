# Workspace Switcher

A simple script to quickly switch between workspaces in i3 window manager using `dmenu`.

## Features
- Switch to workspaces by number or name.
- Simple to install and use.
- Compatible with i3 window manager.

## Installation
1. Clone this repository:
   ```bash
   git clone https://github.com/yourusername/workspace-switcher.git
